<!DOCTYPE html>
<html>
<head>
    <title>{{ customer.name }} - Мой Бизнес</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h1 class="mb-4">👤 {{ customer.name }}</h1>
        
        <a href="/customers" class="btn btn-secondary mb-4">← Назад к клиентам</a>

        <!-- Информация о клиенте -->
        <div class="row mb-4">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-body">
                        <h5>Информация о клиенте</h5>
                        <table class="table">
                            <tr>
                                <td><strong>Имя:</strong></td>
                                <td>{{ customer.name }}</td>
                            </tr>
                            {% if customer.phone %}
                            <tr>
                                <td><strong>Телефон:</strong></td>
                                <td>📞 {{ customer.phone }}</td>
                            </tr>
                            {% endif %}
                            {% if customer.city %}
                            <tr>
                                <td><strong>Город:</strong></td>
                                <td>🏙️ {{ customer.city }}</td>
                            </tr>
                            {% endif %}
                            <tr>
                                <td><strong>Клиент с:</strong></td>
                                <td>{{ customer.created_at.strftime('%d.%m.%Y') }}</td>
                            </tr>
                        </table>
                    </div>
                </div>
            </div>
            
            <div class="col-md-6">
                <div class="card">
                    <div class="card-body text-center">
                        <h5>Статистика</h5>
                        <div class="row">
                            <div class="col-6 mb-3">
                                <div class="text-primary" style="font-size: 2rem;">{{ stats.total_orders }}</div>
                                <small>Заказов</small>
                            </div>
                            <div class="col-6 mb-3">
                                <div class="text-success" style="font-size: 2rem;">{{ "%.0f"|format(stats.total_sales) }} TJS</div>
                                <small>Выручка</small>
                            </div>
                            <div class="col-6">
                                <div class="text-info" style="font-size: 2rem;">{{ "%.0f"|format(stats.total_profit) }} TJS</div>
                                <small>Прибыль</small>
                            </div>
                            <div class="col-6">
                                <div class="text-warning" style="font-size: 2rem;">
                                    {% if stats.total_orders > 0 %}
                                    {{ "%.0f"|format(stats.total_sales / stats.total_orders) }} TJS
                                    {% else %}
                                    0 TJS
                                    {% endif %}
                                </div>
                                <small>Средний чек</small>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- История заказов -->
        <div class="card">
            <div class="card-body">
                <h5>История заказов</h5>
                {% if customer.sales %}
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>Дата</th>
                                <th>Товар</th>
                                <th>Цена продажи</th>
                                <th>Прибыль</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for sale in customer.sales %}
                            <tr>
                                <td>{{ sale.sale_date.strftime('%d.%m.%Y') }}</td>
                                <td>{{ sale.product.name }}</td>
                                <td>{{ "%.2f"|format(sale.sale_price) }} TJS</td>
                                <td class="{% if (sale.sale_price - calculate_cost_price(sale.product)) > 0 %}text-success{% else %}text-danger{% endif %}">
                                    {{ "%.2f"|format(sale.sale_price - calculate_cost_price(sale.product)) }} TJS
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
                {% else %}
                <p class="text-muted">У клиента пока нет заказов</p>
                {% endif %}
            </div>
        </div>
    </div>
</body>
</html>