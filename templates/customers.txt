<!DOCTYPE html>
<html>
<head>
    <title>Клиенты - Мой Бизнес</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .customer-card { transition: transform 0.2s; }
        .customer-card:hover { transform: translateY(-5px); }
        .stats-number { font-size: 1.5rem; font-weight: bold; }
    </style>
</head>
<body>
    <div class="container mt-4">
        <h1 class="mb-4">👥 Клиенты</h1>
        
        <a href="/" class="btn btn-secondary mb-4">← Назад к заказам</a>

        <!-- Форма добавления клиента -->
        <div class="card mb-4">
            <div class="card-body">
                <h5>Добавить нового клиента</h5>
                <form method="POST" action="/add_customer">
                    <div class="row g-3">
                        <div class="col-md-3">
                            <input type="text" name="name" class="form-control" placeholder="Имя клиента" required>
                        </div>
                        <div class="col-md-3">
                            <input type="text" name="phone" class="form-control" placeholder="Телефон">
                        </div>
                        <div class="col-md-3">
                            <input type="text" name="city" class="form-control" placeholder="Город">
                        </div>
                        <div class="col-md-3">
                            <button type="submit" class="btn btn-primary">Добавить клиента</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>

        <!-- Список клиентов -->
        <div class="row">
            {% for customer in customers %}
            <div class="col-md-6 col-lg-4 mb-4">
                <div class="card customer-card h-100">
                    <div class="card-body">
                        <h5 class="card-title">{{ customer.name }}</h5>
                        
                        {% if customer.phone %}
                        <p class="card-text mb-1">📞 {{ customer.phone }}</p>
                        {% endif %}
                        
                        {% if customer.city %}
                        <p class="card-text mb-3">🏙️ {{ customer.city }}</p>
                        {% endif %}
                        
                        <div class="row text-center">
                            <div class="col-4">
                                <div class="stats-number text-primary">{{ customer.total_orders }}</div>
                                <small>Заказов</small>
                            </div>
                            <div class="col-4">
                                <div class="stats-number text-success">{{ "%.0f"|format(customer.total_sales) }} TJS</div>
                                <small>Выручка</small>
                            </div>
                            <div class="col-4">
                                <div class="stats-number text-info">{{ "%.0f"|format(customer.total_profit) }} TJS</div>
                                <small>Прибыль</small>
                            </div>
                        </div>
                        
                        <div class="mt-3">
                            <small class="text-muted">
                                Клиент с {{ customer.created_at.strftime('%d.%m.%Y') }}
                            </small>
                        </div>
                    </div>
                    <div class="card-footer">
                        <a href="/customer/{{ customer.id }}" class="btn btn-outline-primary btn-sm">Подробнее</a>
                    </div>
                </div>
            </div>
            {% else %}
            <div class="col-12">
                <div class="text-center text-muted py-5">
                    <h5>Пока нет клиентов</h5>
                    <p>Добавьте первого клиента через форму выше</p>
                </div>
            </div>
            {% endfor %}
        </div>

        <!-- Общая статистика -->
        {% if customers %}
        <div class="card mt-4">
            <div class="card-body">
                <h5>Общая статистика по клиентам</h5>
                <div class="row text-center">
                    <div class="col-md-3">
                        <h3 class="text-primary">{{ customers|length }}</h3>
                        <p>Всего клиентов</p>
                    </div>
                    <div class="col-md-3">
                        <h3 class="text-success">
                            {{ "%.0f"|format(customers|sum(attribute='total_sales')) }} TJS
                        </h3>
                        <p>Общая выручка</p>
                    </div>
                    <div class="col-md-3">
                        <h3 class="text-info">
                            {{ "%.0f"|format(customers|sum(attribute='total_profit')) }} TJS
                        </h3>
                        <p>Общая прибыль</p>
                    </div>
                    <div class="col-md-3">
                        <h3 class="text-warning">
                            {{ customers|sum(attribute='total_orders') }}
                        </h3>
                        <p>Всего заказов</p>
                    </div>
                </div>
            </div>
        </div>
        {% endif %}
    </div>
</body>
</html>